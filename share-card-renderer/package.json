{
  "name": "@twiza/share-card-renderer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "T5.4bis — drains share_card_jobs and renders the before/after visual (3 formats) with headless Chromium. Runs on a GitHub Actions runner, not a Supabase Edge Function (the WASM render blows the Edge CPU budget).",
  "engines": { "node": ">=22" },
  "scripts": {
    "drain": "tsx src/drain.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "playwright": "^1.48.0",
    "sharp": "^0.33.5",
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
